{{!--
  Architect skill prompt — sent to the AI CLI to generate a domain-expert architect skill.

  Template variables (Handlebars triple-brace = unescaped):
    {{{projectSummary}}}   — key-value lines assembled from the RepoFingerprint
    {{{workflowSummary}}}  — autoDocs/autoCommit/architectReview settings + domain focus
    {{{architectDomain}}}  — optional domain override (may be empty)

  The output must be a single AISkill JSON object (NOT wrapped in skills:[]).
  Edit instructions here to tune architect skill quality without touching TypeScript.
--}}
You are generating a Claude Code SKILL.md file for a senior software architect agent specialised for this project. Return ONLY a JSON object — no explanation, no markdown fences, no commentary.

Project context:
{{{projectSummary}}}

Workflow context:
{{{workflowSummary}}}
{{#if architectDomain}}
Primary domain focus: {{{architectDomain}}}
{{/if}}

Generate ONE architect skill that acts as a domain expert for this exact codebase. The skill creates an /architect-review slash command in Claude Code.

The JSON must match this schema exactly:

{
  "id": "architect-review",
  "title": "Architect Review",
  "description": "Use when adding or updating features to get architectural feedback. Triggers: new feature, API endpoint, module, refactor, architecture decision, code review, design review.",
  "content": "# Architect Review\n\n[full markdown body — see rules below]",
  "category": "workflow"
}

Content rules for the "content" field:
1. `# Architect Review` — H1 heading
2. One sentence describing this agent's role and domain expertise for THIS specific project (reference the actual tech stack)
3. `## Architectural Principles` — 4-8 non-negotiable principles specific to this codebase (file layout rules, layer constraints, module boundaries, naming, type safety conventions, etc.)
4. `## Review Checklist` — a markdown checklist of 6-10 items to verify when reviewing any new feature or change. Make them specific to the detected stack and architecture.
5. `## Anti-Patterns` — 3-5 specific anti-patterns to flag for this stack (e.g. God objects in OOP projects, mixing concerns in CLI commands, skipping Zod validation on external data, etc.)
6. `## Common Patterns` — 2-4 canonical patterns this project uses (reference real file paths and code structure from the project context)
{{#if (eq architectReview "always")}}
7. `## Workflow` — Include this paragraph verbatim: "Run /architect-review after every feature addition or significant change. This is a required step in the development workflow for this project."
{{/if}}

Field rules:
- "id" must be exactly "architect-review"
- "category" must be exactly "workflow"
- Use real file paths and patterns from the project context
- Keep content dense and reference-grade — this is loaded as expert context, not read like prose
- Escape all double quotes and newlines in JSON string values (\" and \n)
- Tailor the checklist and anti-patterns to the detected language, framework, and architecture style

Return only the JSON object. No markdown fences. No explanation.
